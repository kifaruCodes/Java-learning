public class Main {
    public static void main(String[] args) {
        // OOP learning project, part 2

        // Modifiers
        /*
        *   Modifiers are used to set accessibility of classes, methods, constructors, attributes etc
        *
        *   There are 12 modifiers:
        *       - private : accessible only within the same class, not outside
        *       - default : accessible only within the same package. If modifier not specified, it will be set as default
        *       - protected : Accessible within the same package. Accessible outside the package via a child class, hence inheritance is essential for this to work.
        *       - public : Accessible anywhere. Accessible within & outside  both a class and/or package
        *       - final :
        *       - synchronized :
        *       - abstract :
        *       - native :
        *       - strictfp :
        *       - transient :
        *       - volatile :
        *
        *   There are two types of modifiers :
        *       - access modifiers : define boundaries of accessing a class or contents of a class
        *       - non access modifiers
        * */

        /**
         * r
         */


    }
}
